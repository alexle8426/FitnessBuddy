# Fitness Buddy Daily Log - 2026-07-13

## Today

- Fixed the nutrition entry field so hardware keyboard Return/Enter can trigger logging.
- Kept focus in the food note field after keyboard logging so repeated note-style entry feels faster.
- Made the visible `Log` pill appear only after the user has typed food text, reducing idle-screen clutter.
- Added a small meal metadata chip for logged nutrition rows after a food has been assigned to Breakfast, Lunch, Dinner, or Snacks.
- Kept meal grouping out of the main Nutrition tab layout; assignment still happens from the food detail sheet.
- Added an explicit SwiftData save when editing a logged food in the detail sheet.
- Added a post-log meal assignment prompt so newly logged foods can be grouped immediately or skipped.
- Added compact Photo and Barcode entry affordances in Auto mode with placeholder sheets for future camera/scanner integration.
- Added a grouped Meal History sheet from the Today pill with Breakfast, Lunch, Dinner, Snacks, unassigned entries, and the daily macro pill.
- Added Profile/Settings groundwork from the streak/settings pill:
  - local AI estimate bias slider
  - current macro target summary
  - placeholder copy for future AI macro planning
- Polished Workout tab basics:
  - hardware Return/Enter can log workouts
  - workout logging keeps focus for repeated entry
  - explicit SwiftData saves for add/delete
  - custom swipe-to-delete rows
  - simple `Last:` hint for repeated exercises based on matching exercise name text

## Product Notes

- Main Nutrition tab should stay notes-clean: no permanent Breakfast/Lunch/Dinner/Snacks sections on the home surface.
- Meal grouping is now visible only as subtle metadata on rows that have actually been assigned.
- The `Log` button is still available for touch, but the UI leans more toward keyboard/notes-style entry.
- Today pill now opens the grouped meal history surface.
- Streak/settings pill now opens the Profile/Settings scaffold.

## Still To Do

- Test hardware Return/Enter on the actual simulator/device flow, not just compile coverage.
- Decide whether the post-log meal assignment prompt feels too interruptive.
- Replace placeholder Photo and Barcode sheets with actual camera/scanner integrations.
- Split `DailyLogView.swift` into separate Nutrition, Workout, Settings, and History files before it grows further.
- Expand Settings/Profile:
  - real macro goal editing
  - AI macro plan creation
  - account/login/onboarding path
- Improve workout parsing beyond simple exercise-name matching.
- Add a real history tab or navigation path when the app structure is ready for more than two tabs.

## Verification

- `xcodebuild -project 'Fitness Buddy.xcodeproj' -scheme 'Fitness Buddy' -destination 'generic/platform=iOS Simulator' build` passed.
