# Fitness Buddy Daily Log - 2026-07-12

## Today

- Continued the nutrition-first UX pivot toward a clean Apple Notes-style daily log.
- Kept the Nutrition tab simple: top header, compact Auto/Manual switch, full-page food note entry, logged-food notes list, and floating macro pill.
- Made typed food entries appear immediately in the logged-food list after logging.
- Added a local pending-row fallback so newly submitted entries show without waiting on SwiftData query refresh.
- Added explicit SwiftData saves for creating and deleting nutrition log entries.
- Added swipe-to-delete for custom note-style logged-food rows.
- Fixed the delete affordance so the trash icon only appears after swiping, not under the `Thinking` status.
- Updated manual calorie rows to use the flame icon instead of the `#` number symbol.
- Updated the expanded macro breakdown sheet to match the bottom macro pill:
  - calories keep the flame icon and calorie color
  - carbs use `C` and carbs color
  - protein uses `P` and protein color
  - fat uses `F` and fat color

## Current Known Issue

- Pressing Enter/Return from the laptop keyboard does not always log the entry. The on-screen `Log` button and keyboard toolbar action work, but hardware keyboard submit behavior needs a pass.

## Tomorrow

- Fix hardware keyboard Enter/Return behavior for food logging.
- Decide whether the visible `Log` pill should stay or become more subtle once keyboard submission is reliable.
- Tighten the food entry flow:
  - Auto mode: free-text food description, status starts as `Thinking`, AI estimate later.
  - Manual mode: exact calories/macros entry with clean macro fields.
  - Future inputs: photo and barcode entry, likely as small actions near the entry surface or a focused add sheet.
- Add meal grouping support without cluttering the main Nutrition tab:
  - assign Breakfast, Lunch, Dinner, Snacks from entry detail or post-log prompt
  - add a separate grouped/history view later
- Improve the expanded macro sheet:
  - polish spacing and progress bars
  - decide whether over-target macros should use a different visual state
- Start Settings/Profile groundwork:
  - AI estimate bias slider belongs here
  - macro goal plan creation later
  - streak/history tab later
- Keep Workout tab in mind for later:
  - manual typed workouts
  - remember previous exercise performance
  - support simple progressive overload cues

## Verification

- `xcodebuild -project 'Fitness Buddy.xcodeproj' -scheme 'Fitness Buddy' -destination 'generic/platform=iOS Simulator' build` passed after today’s changes.
